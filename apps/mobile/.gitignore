# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli
# @end expo-cli
.env.local  # never commit secrets
.env.development
.env.staging
.env.production # committed seed in migrations/016 (see STACK.md)
.env.example     # example env file
.expo/           # generated by expo-cli
expo-env.d.ts    # generated by expo-cli
dist/            # generated by expo-cli
node_modules/    # generated by pnpm
.pnpm-lock.yaml  # generated by pnpm
.pnpm-workspace.yaml  # generated by pnpm
.pnpm-workspace.yaml  # generated by pnpm   eas.json     # committed? no, keep in repo for now per plan but secrets not
.sentryclirc  # committed? no, keep in repo for now per plan but secrets not
posthog*.json  # committed? no, keep in repo for now per plan but secrets not
.worktrees/  # committed? no, keep in repo for now per plan but secrets not
worktrees/  # committed? no, keep in repo for now per plan but secrets not
ruvector.db  # committed? no, keep in repo for now per plan but secrets not
agentdb.rvf  # committed? no, keep in repo for now per plan but secrets not
agentdb.rvf.lock  # committed? no, keep in repo for now per plan but secrets not
pnpm  # committed? no, keep in repo for now per plan but secrets not    tmp/         # generated by expo-cli
        #           # temp/          # generated by expo-cli
        #           # logs/          # generated by expo-cli
        #           # coverage/      # generated by expo-cli
        #           # .DS_Store      # generated by expo-cli
        #           # .vscode/       # generated by expo-cli
        #           # .idea/        # generated by expo-cli
        #           # .cursor/      # generated by expo-cli
        #           # .sublime-project  # generated by expo-cli
        #           # .sublime-workspace  # generated by expo-cli
.env

