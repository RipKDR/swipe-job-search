// Apple Sign-In is enabled on iOS via Supabase OAuth (native flow handled
// by `expo-web-browser`). On Android/web the button is hidden by default.
export const APPLE_AUTH_ENABLED = true;
export const APPLE_AUTH_DISABLED_COPY =
  'Apple sign-in will be enabled in production once credentials are configured.';
