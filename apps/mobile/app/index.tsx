import { LoadingScreen } from '@/components/ui/LoadingScreen';

// Root index — root layout's useEffect handles redirect to the correct tab home.
export default function Index() {
  return <LoadingScreen />;
}
